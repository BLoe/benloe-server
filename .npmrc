# Same fix as apps/cabinet/.npmrc, applied at the outer monorepo workspaces
# root too: ambient NODE_ENV=production makes npm silently omit ALL
# devDependencies on install/ci, with no error. Every app under apps/ shares
# this agent shell, so every app shares the risk. See PLATFORM.md, 2026-07-16.
include=dev
